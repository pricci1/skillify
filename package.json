{
  "name": "skillify",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "skillify": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build:scripts": "bun scripts/bundle-mcp-client.ts",
    "build:cli": "bun scripts/bundle-cli.ts",
    "build": "bun run build:scripts && bun run build:cli",
    "prepublishOnly": "bun run build",
    "dev": "bun run src/index.ts",
    "typecheck": "bun run build:scripts && tsc --noEmit"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.1.0",
    "@modelcontextprotocol/sdk": "^1.25.1",
    "commander": "^14.0.2",
    "ejs": "^3.1.10",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ejs": "^3.1.5",
    "@types/node": "^25.0.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
